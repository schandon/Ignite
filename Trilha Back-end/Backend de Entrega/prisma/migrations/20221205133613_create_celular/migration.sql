-- CreateTable
CREATE TABLE "celular" (
    "id" TEXT NOT NULL,
    "model" TEXT NOT NULL,
    "no_" TEXT NOT NULL,
    "so" TEXT NOT NULL,
    "adm_aproved" BOOLEAN NOT NULL,

    CONSTRAINT "celular_pkey" PRIMARY KEY ("id")
);
